{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @nextorders/food-schema build && pnpm --filter @nextorders/essence build && rm -rf .vercel/output && mkdir -p .vercel && cp -r apps/essence/.vercel/output .vercel/output"
}
